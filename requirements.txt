fastapi==0.111.0
uvicorn==0.30.1
# langchain-openai 要 openai>=2.45，所以这里跨了大版本。
# 注意 openai SDK 不只模型层在用——app/tools/web.py 的 DashScopeProvider
# 直接拿它做联网搜索（extra_body={"enable_search": True}），升级要一并真机验。
openai==2.53.0
langgraph==1.2.10
langchain-openai==1.4.1
langgraph-checkpoint-sqlite==3.1.1
# 需要 Python 3.12+。
# 这条锁定的原始理由（6.x 的 Rust 扩展没有 py38 wheel）在 3.12 上已经不成立，
# 升级路径是通的，只是还没升——升的时候注意 6.x 起 DDGS() 的用法有变化。
duckduckgo-search==5.3.1
pytest==8.2.0
httpx==0.27.0
