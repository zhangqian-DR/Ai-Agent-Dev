fastapi==0.111.0
uvicorn==0.30.1
# langchain-openai 要 openai>=2.45，所以这里跨了大版本。
# 注意 openai SDK 不只模型层在用——app/tools/web.py 的 DashScopeProvider
# 直接拿它做联网搜索（extra_body={"enable_search": True}），升级要一并真机验。
openai==2.53.0
langgraph==1.2.10
langchain-openai==1.4.1
langgraph-checkpoint-sqlite==3.1.1
# duckduckgo-search 已改名为 ddgs（8.1.1 只是个会打警告的过渡壳）。
# ddgs 自己聚合多个搜索源，从这台机器上实测能搜到，而旧包一搜就 Ratelimit。
# 用法与字段（DDGS() 上下文管理器、.text(q, max_results=n)、title/href/body）一致。
ddgs==9.14.4
pytest==8.2.0
# ddgs 要 >=0.28.1。验过 FastAPI 的 TestClient 在 0.28 上照常工作。
httpx==0.28.1
