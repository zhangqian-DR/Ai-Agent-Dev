fastapi==0.111.0
uvicorn==0.30.1
openai==1.40.0
# 6.x 依赖 pyreqwest-impersonate，Python 3.8 无预编译 wheel，装它要现场编 Rust。
# 5.3.1 有 py38 wheel，DDGS() 上下文管理器 + .text(query, max_results=) 的用法一致。
# 升级到 Python 3.10+ 后可以直接换回 6.2.0。
duckduckgo-search==5.3.1
pytest==8.2.0
httpx==0.27.0
